/**
 * Banner - 홈 화면 배너
 *
 * 역할:
 * - 정적/단순 배너 컴포넌트
 * - 데이터 fetching 없음
 *
 * TODO(이경환): 디자인 확정 후 구현
 */
export function Banner() {
  return (
    <section>
      <div>Banner placeholder</div>
    </section>
  );
}
